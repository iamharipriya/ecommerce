<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Cart</title>
   
    <style>
        /* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Container to center products */
body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    
    min-height: 100vh;
}

/* Main Product Container */
.product-container {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    /* align-items: center; */
    gap: 20px;
    width: 80%;
    max-width: 1200px;
    margin-top: 50px;
    /* display: flex;
    justify-content: center;
    align-items: center; */
}

/* Product Card Styling */
.product-card {
    background-color: #fff;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    text-align: center;
    transition: transform 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    /* height: 400px; */
}

/* Hover effect on product card */
.product-card:hover {
    transform: translateY(-10px);
}

/* Image inside the product card */
.product-img {
    /* width: 100%; */
    /* height: 200px; */
    object-fit: cover;
    border-bottom: 1px solid #ddd;
    
}

/* Product Details */
.product-name {
    font-size: 1.5rem;
    font-weight: bold;
    margin: 15px 0;
}

.product-description {
    font-size: 1rem;
    color: #555;
    padding: 0 15px;
    margin-bottom: 20px;
}

/* Product Price */
.product-price {
    font-size: 1.25rem;
    color: #333;
    font-weight: bold;
    margin-bottom: 20px;
}

/* Add to Cart Button */
.add-to-cart {
    background-color: #5cb85c;
    color: white;
    border: none;
    padding: 10px;
    width: 80%;
    font-size: 1rem;
    cursor: pointer;
    border-radius: 5px;
    margin-bottom: 15px;
    transition: background-color 0.3s ease;
}

/* Button Hover effect */
.add-to-cart:hover {
    background-color: #4cae4c;
}

/* Responsive Design */
@media screen and (max-width: 900px) {
    .product-container {
        grid-template-columns: repeat(2, 1fr);
    }
}

@media screen and (max-width: 600px) {
    .product-container {
        grid-template-columns: 1fr;
    }

    .product-card {
        height: auto;
    }
}
.navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #007bff;
      color: white;
      padding: 10px 20px;
    }
    .navbar .logo {
      font-size: 1.5rem;
      font-weight: bold;
    }
    .navbar ul {
      display: flex;
      list-style: none;
      margin: 0;
      padding: 0;
    }
    .navbar ul li {
      margin: 0 15px;
    }
    .navbar ul li a {
      text-decoration: none;
      color: white;
      font-size: 1rem;
      transition: color 0.3s;
    }
    .navbar ul li a:hover {
      color: #ffd700;
    }

    /* Responsive Menu */
    .menu-toggle {
      display: none;
      flex-direction: column;
      cursor: pointer;
    }
    .menu-toggle span {
      height: 3px;
      width: 25px;
      background: white;
      margin: 4px 0;
    }

    /* Mobile View */
    @media (max-width: 768px) {
      .navbar ul {
        display: none;
        flex-direction: column;
        background-color: #007bff;
        position: absolute;
        top: 60px;
        left: 0;
        width: 100%;
      }
      .navbar ul.active {
        display: flex;
      }
      .menu-toggle {
        display: flex;
      }
      .navbar ul li {
        text-align: center;
        margin: 10px 0;
      }
    }
    .ad{
      display: flex;
      justify-content: center;
    }

    </style>
</head>
<body>
    <nav class="navbar">
        <div class="logo">E-Commerce</div>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/">Products</a></li>
          <li><a href="/cart">Cart</a></li>
          <li><a href="/login">Login</a></li>
          <li><a href="/signup">Sign Up</a></li>
        </ul>
        <div class="menu-toggle" onclick="toggleMenu()">
          <span></span>
          <span></span>
          <span></span>
        </div>
      </nav>
    
    <div class="product-container">
        <!-- Product 1 -->
        <% pct.forEach(pct=>{ %>
        <div class="product-card">
          <div class="product-img">
            <img src='<%= pct.pctimg %>' alt="Product 1"  width='100px' height='100px'>
            </div>
            <h3 class="product-name"><%=pct.pctname %></h3>
            <dl>
              <dt class="product-description"><%= pct.pctdis %></dt>
            </dl>
            <p class="product-price">₹<%= pct.pctprice %></p>
            <div class="ad">
             <form action="/addcart/<%= pct._id %>" method="GET" style="display: inline;">
                  <button class="add-to-cart">Add to Cart</button>
             </form> 
            </div>
        </div>
        <% }) %>
    </div>
    <script>
        // Toggle menu for mobile
        function toggleMenu() {
          const menu = document.querySelector('.navbar ul');
          menu.classList.toggle('active');
        }
      </script>
</body>
</html>
